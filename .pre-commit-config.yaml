repos:
-   repo: local
    hooks:
    -   id: pylint
        name: pylint
        entry: pylint
        language: system
        types: [python]
        args: [
          "rn", #only display the error code and message
          "-sn", #don't display the name of the checker
          "--rcfile=.pylintrc", #link to the pylint configuration file
          "--load-plugins=pylint.extensions.docparams", #load an extension
        ]

 